
@profile
def give_actions(actions: List[int]) -> List[int]:
    """
    NOTE: Suggested, not necessary?
    Give the actions permutations that can come from a list of actions
    :param actions:
    :return:
    """
    return ''
